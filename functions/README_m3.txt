Functions note
- Current index.ts (Milestone 2) already recomputes nextFireUtc and logs CREATE/UPDATE.
- For proposals, Android applies approval (writes alarm) and deletes proposal.
- Optional: add a new Cloud Function to send FCM notifications on proposal create / approval when you enable FCM.
